{
  "name": "subgraph-basix",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ n4sunday/subgraph-basix",
    "create-local": "graph create --node http://localhost:8020/ n4sunday/subgraph-basix",
    "remove-local": "graph remove --node http://localhost:8020/ n4sunday/subgraph-basix",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 n4sunday/subgraph-basix"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
